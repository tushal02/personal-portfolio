// Example to add the social media icons and links dynamically
document.addEventListener("DOMContentLoaded", function () {
    setTimeout(function () {
      // Add the "show-social" class to the footer after a delay or based on any condition
      document.querySelector("footer").classList.add("show-social");
    }, 1000);  // Delay of 1 second (can be changed or adjusted as needed)
  });

